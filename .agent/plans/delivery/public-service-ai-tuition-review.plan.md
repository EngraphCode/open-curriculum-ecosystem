---
id: public-service-ai-tuition-review
node_type: delivery
name: Independent review of the public-service AI tuition import (PR 66)
overview: One review lane over PR 66 — import fidelity against ADR-226, an audit of the published author-lineage review, and the findings neither surface could make — landing a report, one typed review on the PR, and the forward obligations the owner decides.
status: sketch
ratified_by: null
ratified_date: null
ratified_where: null
serves: planning-and-intent-estate
impact_areas:
  - practice-and-estate
tickets: []
depends_on: []
owner_gates:
  - awaiting: owner-decision
    clears_when: >-
      The owner releases or refuses PR 66's draft hold with the report's split in hand: the
      historical runner's cure (convert to TypeScript per the earlier import's precedent, keep only
      the results JSON and cite the runner's source archive, or rule that a research record's
      executable bytes are data — a ruling that also settles the eleven hand-authored shell and
      Python files already under the research surface), and the visibility of the nine oaknational
      repositories the collection permalinks into (ADR-226 clause 2; unverified by construction
      because this line reads no Oak surface without the owner's word).
    expires: 2026-09-27
  - awaiting: owner-decision
    clears_when: >-
      The owner states whether the collection's "settled" service, POC and managed-platform
      directions are settled at their word and where that word lives, and whether the chapter-13
      work programme receives a plan node of its own and under which strategic parent.
    expires: 2026-09-27
last_updated: 2026-09-06
---

# Independent review of the public-service AI tuition import (PR 66)

## Goal

PR 66 (branch `docs/public-service-ai-tuition-2026-09-06`; collection at SHA:5f04e4d0f, PR head
SHA:2db74f5bb) can be released or refused by the owner on first-hand evidence from a review
independent of the collection's author lineage: every import-fidelity property that the PR's own
validation surface cannot see has a verdict, every finding of the published author-lineage review
is mapped as accepted, partially accepted or refuted, and the decisions that are the owner's reach
the owner as one packet with the factors attached.

## User groups and value

- **The owner**: a hold decision that can be made from one card, with the two owner-only choices
  (the runner's cure; the external-repository visibility) and the settled-direction question
  carrying their factors rather than a recommendation.
- **The collection's author session**: the defects that can only be fixed at source (references
  by the fork's former repository name, a flat "settled" the collection's own chapter 12 grades
  as relayed, the seven content corrections the author-lineage review names, a planning chapter
  that never routes to the inclusion and rights chapters) returned with file and line, never
  edited in the byte-faithful files.
- **Future readers of the research surface** (agents and people): an OCE-side statement of what the
  collection's location does and does not confer, named as a factor with its home, so authority is
  not acquired by directory placement.
- **The estate**: the first review record for an ADR-226 import, with reproduction commands, and
  the two blind spots both reviews shared (external links; executables no gate reaches) named for
  the import procedure. Value offered, not need-proven: the procedure gains two lines only if the
  owner adopts them.

## Mechanism

A seat opened directly by the owner, under the estate's start-right-team foundation, with a
machine-local plan reviewed by a four-leg fleet-design review before pricing. The review's object
is the PR plus the author-lineage review that entered it; its instruments are seat-side
deterministic checks over the PR branch (an external-URL census, the internal-link count, the
runner's syntax, the workbook's zip integrity and formula census, the scanner reproduced in git
mode with token- and path-scoping controls, the tooling-coverage probes), a protected four-leg
frame gate adjudicated before any further spend, one audit leg per finding of the published
review, seat-inline checks of the engineering defect statements at the pinned revision and at one
pinned engraph tip, a correlated audit leg over the draft, and the seat's synthesis under the
Parallax contract (charter, frame set, conflict ledger, epistemic profile, world-return). Findings
route by type: cure-at-source to the author, decision-with-factors to the owner, already-homed to
the OCE record that owns it, disposition-as-intended with the control that proved it.

## Acceptance criteria (each with a proof)

1. **The report exists and is reproducible** —
   `.agent/reports/public-service-ai-tuition/pr-66-independent-review-2026-09-06.md` carries a
   review contract, a review-mapping table over the published review's seven findings, the seat's
   own findings, and a command for every count. Proof `repo-safe`: the lane PR's CI (markdownlint,
   prettier, `pnpm check:docs`) is green and every command in the report re-runs from a clean
   checkout of the PR branch.
2. **One typed review is posted on PR 66** — every finding carries two verdicts (correctness,
   alignment), an evidence class, and one of the five action types; the reviewer opens no tally
   and declares no intake budget. Proof `repo-safe`: the PR's review list shows exactly one review
   by this seat's bot identity with the findings enumerated.
3. **The owner packet is one card** — the runner's cure, the nine repositories' visibility and the
   settled-direction question travel to the owner as one packet raised by the Director, with this
   lane's factors attached. Proof `owner-held`: the Director's record names the packet; the owner's
   answer lands as the two gates above clearing.
4. **Nothing in the 17 imported files changed** — the lane PR's diff touches no path under
   `.agent/research/public-service-ai-tuition/`. Proof `repo-safe`: `git diff --stat` of the lane
   PR against `origin/engraph` lists only this lane's own paths.
5. **The removed fleet stages are recorded, not silently dropped** — the dated proportionality
   note below stands on this node and the spend actuals stand on the report beside the estimate.
   Proof `repo-safe`: both sections present in the landed files.

## Todos

- One single-story PR, archival/record class (PDR-132 §3: exempt from the size warnings, bound by
  the two-round budget): this node, the thread record and its continuity row, the report with its
  three Parallax assets, and one pointing row in `.agent/reports/README.md`. Lands in the slot
  order the Director holds (#58, #67, then this lane).
- The review on PR 66 is posted from the seat's bot identity after the full Cricket panel at the
  report-draft boundary; it is not part of the lane PR's diff.

## Out of scope

- Editing any of the 17 imported files (ADR-226 clause 2 keeps their bytes; findings route to the
  author's source).
- Planning or ratifying the chapter-13 work programme, designing the POC, or verifying the
  education literature and the experiment design on their merits (a research programme, named as a
  factor for the second gate).
- Re-running the historical probe: the published review re-executed it at the pin with all eleven
  probes passing and matching hashes; this lane records that result and checks the figures against
  the results JSON rather than spending a second run.
- Any read of an Oak surface: the nine repositories' visibility is an owner fact and stays
  unverified by construction until the owner's word.
- Merging PR 66 or shepherding it: the draft hold and the PDR-140 intake declaration are the
  owner's; the missing declaration is itself one of this review's findings.
- The full engineering claim ledger (one verdict per Appendix A row and graph-suite assertion):
  named as a separately priced follow-up the owner can buy after the sampled error rate.

## Review dispositions

No rows: this section carries only findings routed off this lane's own PR. PR 66's findings live
on PR 66 and in the report.

## Dated notes

- **2026-09-06, proportionality finding at pickup.** The owner-approved lane plan (machine-local,
  revision 2, band 2.5–4M tokens, cap 5M) priced a mapping, verification and refutation fleet over a
  corpus nobody had read. Before dispatch the author-lineage review entered the PR at
  SHA:2db74f5bb (qualified; seven findings; the runner re-executed at the pin; the workbook
  reconciled). The investigation stop gate re-run: no further claim verification changes the hold
  decision, which the runner verdict and the repositories' visibility already decide at the
  owner's level. The mapping, per-claim verification and refutation stages are therefore not run;
  the run that stands is the frame gate (four legs), one audit leg per published finding, the
  seat's inline checks, one correlated audit leg over the draft and a completeness critic — about
  0.8M tokens, cap 1.2M, approved by the Director under the overnight contract at 20:57Z. This is
  "smaller than treated", never "small enough to do badly"; correctness and strictness are
  unchanged.
- **2026-09-06, the seat verdict on the runner.** ADR-226 clause 2 grants byte-faithfulness to a
  record's documents and prose formatting; clause 4 holds a record's executable content to the
  estate's gates. The hand-authored `.mjs` runner is executable content that no gate reaches
  (eslint's shared ignores do not list the research surface, yet the file sits outside every lint
  program; the stale-script validator scans the subtree; nothing else does), and the sole
  authorised hand-authored-`.mjs` tier (ADR-168 §4, scripts that must run before `pnpm install`)
  does not apply to it. Correctness verdict: non-conforming. The cure is the owner's choice and
  rides the first gate. The eleven hand-authored shell and Python files already under the research
  surface are precedent, not approval, and the same ruling settles them.
- **2026-09-06, after the compaction boundary: one retraction and one shape fact.** The first
  posted review's finding 2 ("31 dead links to a repository that does not exist") is withdrawn:
  the name is the fork's former one, GitHub redirects it, and the same name occurs about sixty
  times on `engraph`; the error was a liveness verdict inferred from a name mismatch and never
  probed (retracted on the PR in the second comment; failure-mode event `ced01103`). The shape
  fact: the fourteen markdown files are one link-closed component under the repository's
  markdown-links validator, so no subset of the prose can be committed or reviewed apart; the
  owner's size-capped `/code-review ultra` can take only the three assets and the non-index
  integration files (about 900 lines) as a local fixture branch, and the rest only whole. The
  R1–R7 mapping is posted (five accept, two partial, none refuted); the report's frame set,
  epistemic profile and conflict ledger are filled; status provisional pending the owner packet.
