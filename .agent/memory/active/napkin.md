---
fitness_line_target: 220
fitness_line_limit: 300
fitness_char_limit: 18000
fitness_line_length: 100
drain_strategy: "Extract settled entries to permanent docs, PDRs, rules, or archived napkins"
merge_class: append-only-narrative
fitness_content_role: drainable-buffer
---

# Napkin

Current-session observations. Append below. Rotate when over ~400 lines (`consolidate-docs`
step 6): extract every behaviour-changing entry, merge into `distilled.md` or graduate to a
permanent home, verify the home, then archive and start fresh. Rotation is the preservation
step AFTER processing — never a fitness-relief move or a queue (owner correction, 2026-07-06).
Author entries per `records-are-technical-not-emotional` §Records carry their authority
honestly: observations get past tense, a named instance, and scope/expiry in the same
sentence; only owner-ratified doctrine gets the imperative. On this shared surface, edit by
exact-match anchor or append-only — never rewrite from a positional marker (truncate-race,
2026-07-28).

## Napkin rotated (2026-09-17, the dedicated consolidation's second context, Zephyr guards Leeward, 281e44, sole operator)

Rotated below the ~400-line threshold at the owner's priority of 2026-09-17, verbatim: "the
priorities are drainable buffers to EMPTY, then memory files to an optimised soft". The window —
the 2026-09-16 rotation record through the 2026-09-17 afternoon blocks of the dedicated
consolidation — is preserved in `archive/napkin-2026-09-17.md`, byte-identical to the napkin
committed at `SHA:b13e23e4e` (blob `23c7671a0` at both paths). Its behaviour-changing entries
were graduated in `SHA:49b6c3ae9` and `SHA:b13e23e4e` before the move; the entries left in the
archive are single observations (a push past a foreground timeout, the board moving during the
pass, reflective passes) that no home needs yet.

Comms: every non-heartbeat event through 2026-09-16T20:45Z was read in the first context, and
from then to this rotation (15:26Z) the stream held only this seat's own events; Dynamo turns
Temper's events from 15:31Z are read in the wrap block below. The watermark is not advanced and no archive
move has run, because the substantive-event mover is still unhomed, as at the 2026-09-10 and
2026-09-16 rotations.

## Zephyr guards Leeward (281e44) — 2026-09-17 ~15:4xZ, the wrap of the consolidation's second context

- **Metacognition (retrospective).** The context ran fold-first by the lifetime rule, then drained
  what the fold freed. Its largest yield came from checking claims before writing them, not from
  the writing: the pre-publication pass (31), the seat's own check of every verifier finding, the
  PDR-008 reading, the route-comment grep, and reading `continuity-practice.md` before recording
  the owner's "missing rotation definitions", which turned a bare gap into a precise one (a
  disposition that forbade archiving and had no trigger). The recurring slip of the context was
  time typed from memory, twice, each caught only by reading the clock after the write.
- **Metacognition (generative): what changed about the work.** The owner moved the target from
  "empty the register" to "buffers to EMPTY, then memory files to an optimised soft", and then
  decided the lifecycle question the seat put ("Graduate, then archive"). The lesson for a
  consolidation is that the order of work follows which buffers the owner means by empty, and
  that a decision gating a bulk act is asked before the act, not after it.
- **Free play (material: this context; one time-box).** Kept, as associations: a claim pass and a
  napkin rotation are the same act at two scales (read everything, move it only once each piece
  has a home, prove nothing was lost); and the fold that gates another seat's landing is a slot
  held by a record, not a PR. Discarded, visibly, on a reason that proved false: "the `--now` flag
  and the directive budget are one instrument gap" was set aside because one looked refusable by
  the CLI and the other a platform question. #153's pre-publication pass found the context
  reading already built (`agent-tools session-metadata`, refused only for this seat's model id;
  F-191), so both are small CLI cures; the association stays a note, not a pattern.
- **Concept exploration: the four-part memory-surface lifecycle.** Observations: the napkin has a
  role, a disposition, a trigger and a proof; continuity surfaces had three of four; the largest
  records had none declared. Problem frame: a surface without a trigger grows however good its
  disposition. Proposal: the lifecycle as a candidate concept node beside the owner's decision in
  the register, so the directive pass writes the four parts, not only the disposition. Warrant:
  this context's evidence. Falsifier: after the directive pass lands a trigger, the large records
  still grow past their thresholds without the step firing.
- **Loss scan and metaloss.** Compressed reasoning: the lifecycle evidence lives in the register
  entry; the PDR-008 reading in F-189; the context-budget decision in F-191 and the resume point.
  Promises: the directive pass, #153's fold for Dynamo turns Temper's lane, and the archive work
  are in the resume point with their order. Attribution: the Oak integration lane's facts are
  Dynamo turns Temper's team-start event, read first-hand, not re-verified. Blind spot: n = 2 from
  15:31Z, and this seat ran no comms watcher until the claims guard refused the wrap commit's
  claim for that reason (F-95); the watcher was armed then, before the commit landed, and the
  claims registry and the stream were read at the wrap. Fence sweep: no Oak surface written; the fork is named only on this fork. Fixed point: a
  third pass re-finds only the two `--now` slips.

## Zephyr guards Leeward (281e44) — 2026-09-17 ~16:1xZ, the wrap of the consolidation's third context

- **What happened.** After compaction, at the owner's word, the seat opened an ARC channel with
  Dynamo turns Temper, registered at n = 2 (PDR-082), and began folding #153 on its cut date
  rather than at the rollover, because the Oak integration lane's slot waits on it (the seat's
  decision, reported to the owner; no word back yet). Four context-free verifiers ran the
  pre-publication claim pass over the branch and the draft description: 33 findings, each
  checked at its source by the seat, all true, all cured for one commit. That commit's `git add`
  hung about ten minutes holding the index lock on a unix socket (fsmonitor IPC is the likely
  reading, not proven: the daemon answered a status probe). The ceremony was stopped, git
  released the lock on exit, and the re-run disables fsmonitor for staging. A peer's `git
  status` on the primary was sleeping in the same window.
- **Metacognition (retrospective).** The pass replicated #152's yield (31 there, 33 here, every
  one true). Two classes dominated. Surviving sentences: the CodeQL re-truing (`f073db95f`) was
  authored before pr-lifecycle's outward-sweep paragraph landed on this branch, and five of the
  doctrine group's nine findings were sentences it left teaching the withdrawn model; the sweep
  binds authoring re-truings, not only review cures. Absence claims: F-191 said no instrument
  reads a session's context usage, and `agent-tools session-metadata` does (it refuses only the
  Opus 5 model id). The seat had searched one tool and a transcript, not the CLI's command list.
- **Metacognition (generative).** What changed: the 30 % rule became readable. The context read
  29.5 % after this compaction (against the same-size 1M window entry), so the owner's
  fresh-context call for the directive pass rests on a reading, not an estimate.
- **Free play (one time-box).** Kept: the verifier that disproved F-191 also handed the seat its
  own context reading, so one claim check paid twice; and a hung `git add` holding the index
  lock looks like progress from outside, as a silently dead watcher does. Discarded, visibly:
  "every hang on the primary is fsmonitor" — two sleeping processes in one window name no cause.
- **Concept exploration: an absence claim is a zero-match reading.** Observations: F-191's "no
  tool reports the figure" and the disposition policy's "no path exclusion exists" were both
  false absence claims in one fold. Existing home: `zero-match-false-green` (a search that finds
  nothing, read as proof of absence). Proposal: no new concept; the instances extend that
  pattern from greps to a seat's own search of a tool surface or a configuration, and the cure
  is to record the search (command and scope) beside the claim. Warrant: two instances, one
  fold, one seat. Falsifier: a fold whose absence claims each carry their search still draws
  absence-class findings. It stays a napkin note until another fold repeats it.
- **Loss scan and metaloss.** Uncommitted at this block's writing: the 33 cures, this block and
  the ARC channel; the next commit carries them. Promises: #153's fold (description update,
  ready, review under budget 2, front door, successor cut, rotation broadcast) is item 1 of the
  resume point; the carrier's landing records are Dynamo turns Temper's to write on the
  successor. Attribution: the peer's sleeping process is read from `ps` (its parent shell ran in
  that seat's carrier-supersede worktree), not confirmed by the peer. Blind spot: the comms
  watcher expired about 16:04Z and was re-armed seeded from now, so the stream between is unread;
  it is read at resume. Fence sweep: no Oak surface written.

## Zephyr guards Leeward (281e44) — 2026-09-17 18:3xZ to 2026-09-19 10:4xZ, the consolidation's fourth context: the #153 fold, and the directive pass gated again

- **What happened.** Resumed after compaction as Fable 5.1 (the owner's switch); the agent id is
  unchanged. The owner answered four cards: fold #153 now; the window is 1M; F-189's framing was
  wrong (the question is which gate names are right for the whole Practice ecosystem, then
  standardising everywhere; this repository's names have been stable for a long time); rename
  the sync names after #154 lands. #153 folded through the front door at 20:17:05Z as
  `SHA:b5b0e70cd` after three rounds (thirteen distinct findings, all true at source: six cured in
  two settlement pushes, one dispositioned on its thread, six routed to the successor and cured
  there in `SHA:84d7bc441`; this block first said "twelve, seven, five", which its own
  pre-publication pass on #155 showed fits neither the distinct nor the raised count). The
  successor `coordination/2026-09-17-b5b0e7` was cut and draft #155 opened. Then the meter read
  51 % of 1M, the owner chose a fresh context for the directive pass, and the answer arrived
  about 38 hours later, so the successor was DUE at pickup and this context folds it.
- **Mistake: the headroom was spent before the meter was read again.** At 18:36Z the meter read
  13.3 % and the seat declared the shape "fold here, directives here". By the directive step it
  read 51 %. The spend: the fold's three rounds (each a fresh harvest of reviews, threads and
  checks) and, larger, end-to-end reads of six directives (about 2,300 lines) made DURING the
  fold's waits, before any edit could happen. consolidate-until-done step 7's prediction ("names
  its contexts at open and finishes its directive entries in a planned context") is falsified a
  second time by this seat: the shape was named at open and the split was still found at the
  end. Cause: the rule budgets the READ as well as the edit, and the seat pre-read to save time
  it then could not use. Behaviour change: in a context that also folds, read the meter after
  the fold's last push and BEFORE any directive read; if it is above about 20 % at that moment,
  the directive reads belong to the next context, and the current one does nothing but the fold
  and the handoff. The drafts written this context (the IO rewrite, the commit-2/3 texts, the
  plan) are conserved in the machine-local handoff record so the reads are not repeated blind.
- **Mistake: the rotation broadcast draft said "one review round" after three.** Caught before
  posting by re-reading the sed-filled file. A template filled by placeholder substitution keeps
  every sentence that was true when the template was written; re-read the whole body, not the
  placeholders.
- **Surprise: a mid-session model relabel collides in the comms route (F-192).** The identity id
  is seeded from the session and survived the model change; `comms append` refused the new label
  against the live identity, while `comms watch` and `assert-watcher-live` accepted it; the claims
  were never tried under the new label, and `claims open` runs the same guard. Kept the old label for
  the session's coordination surfaces and recorded it.
- **Surprise: the owner's answers can arrive days later.** A card asked at 20:2xZ on 09-17 was
  answered on 09-19; every branch-lifetime and claim-freshness assumption in the seat's plan
  expired in between. Behaviour change: at any owner-gated pause, the pause notice names the
  branch's lifetime edge and what the resume does first if it has passed (here: fold before the
  directive pass, which the rule already says).
- **Reviewer classes this fold.** Round one: a decision's surviving phrase (PDR-141 D11 after D7
  was amended), a comment over a mixed list (the gates block), a directive contradicting a ruling
  (AGENT.md §Commands), a criterion over an excluded query (the code-scanning probe). Round two: a
  checklist question contradicting its own paragraph (the Director readiness gate), a plan
  sentence contradicting its own unit. Round three: an over-claim against a mechanism's source
  (the profile push leg), stale index cells, an interim rule that cannot reach terminal zero (the
  carrier tally), an outward-sweep miss on the branch's own cure (pr-lifecycle Phase 4's Sonar
  per-site disposition), an incomplete hook inventory. All true; the pre-publication pass had not
  reached the cross-file consistency of an amendment with its neighbours, nor claims about
  mechanisms. Candidate for pr-lifecycle's claim table: "an amendment's neighbours" and "a
  mechanism claim is read against the code" as named verifier lenses.
- **Metacognition (retrospective).** The seat asked the owner four questions at open and got
  four answers that changed the work (the fold timing, the window, F-189's frame, the naming
  lane); the fifth question (the meter) came at the end because the seat measured only at the
  start. The thing to have measured was the RATE: 13 % at open, three rounds and the reads
  ahead; a second reading after the fold's last push would have stopped the directive reads.
- **Free play (one time-box).** Kept: a fold's review rounds are a good place to read, but only
  what the fold needs; reading for the next task in a gated context is spending someone else's
  budget. Kept: the Codex "0c7b347d" sha was a merge-preview commit that no longer exists; a
  reviewer's sha can name an object GitHub computed and discarded. Discarded, visibly: "the
  meter over-reads because persisted tool output is not in the harness context" — untested, and
  the rule gates on the reading, not on a theory of the reading.
- **Concept exploration: a budgeted read.** Observations: the 30 % rule names the edit; this seat
  and its second context both crossed the line on reads made in service of the edit. Existing
  home: `directive-file-context-budget` says "full-depth reading of the directive file
  end-to-end" is what the headroom is FOR. Proposal: no new concept; the rule's sequencing
  section gains one sentence, "the read is part of the directive work and is budgeted the same
  way: do not pre-read directives in a context that will not edit them" — a directive-pass edit
  for the fresh context, added to the register entry for the rule. Warrant: two contexts of one
  seat. Falsifier: a pass that reads the meter after its fold and before its directive reads
  still finds the split at the end.
- **Loss scan and metaloss.** Uncommitted at this block: this block, the resume-point edits, the
  handoff record (machine-local, untracked by design) and the ARC churn since `SHA:84d7bc441`; the
  wrap commit carries the tracked ones. Promises: the successor folds now (this context), the
  directive pass opens the next context at its directive step from the handoff record; Dynamo
  turns Temper's slot is open and they are paused at the owner's word (claim stale since
  23:31Z 09-17; not this seat's to close). Owner decisions open: F-189's ecosystem-wide names;
  the carrier tally question before #154's rounds. Blind spot: nothing from any seat on the
  stream or the ARC channel since 09-17 20:20Z. Fence sweep: no Oak surface written.
